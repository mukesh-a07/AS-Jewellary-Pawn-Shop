/**
 * AS JEWELLAR PAWN SHOP - GOOGLE APPS SCRIPT API ROUTER
 * Central entry point for all GET / POST operations.
 */

function doGet(e) {
  return Code.doGet(e);
}

function doPost(e) {
  return Code.doPost(e);
}
